# SIP/RTP: the SRS is a pure-Python asyncio SIP UAS (siprec_srs/sip_server.py)
# + RTP recorder (siprec_srs/rtp_recorder.py). No pjsua2 / pjproject needed.
# G.711 decode uses stdlib `audioop`, which moved out of stdlib in 3.13:
audioop-lts; python_version >= "3.13"

# vCon library (>=0.9.1 for spec-correct names: amended, critical)
vcon>=0.9.1

# Configuration and utilities
pyyaml>=6.0
python-dotenv>=1.0.0

# Async HTTP for webhooks
aiohttp>=3.8.0
aiofiles>=23.0.0

# Audio processing
pydub>=0.25.1

# Logging and monitoring
structlog>=23.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-mock>=3.10.0

# Development
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
